# AOT / 裁剪告警的例外表。
#
# 一行一个程序集名。**每条都必须写明为什么可以放行。**
# 只放行第三方程序集——本仓库自己的代码出现任何 IL 告警都必须修，
# 不允许登记进来。写不出理由的，说明它不是例外，是没修。

# Avalonia 官方的 DataGrid 包自身带 IL2104 / IL3053。
# 它是本库唯一的第三方控件依赖（README 里「零第三方依赖」指的是不引社区库，
# Avalonia 官方包不算），我们改不了它的实现。
# 影响面：DataGrid 的列自动生成走反射，裁剪后可能失效——所以本库的
# DataGridToolbar 与展柜示例一律显式声明列，不依赖 AutoGenerateColumns。
Avalonia.Controls.DataGrid
